# Session info (regenerable via setup.R)
docs/session_info.txt

# R session files
.RData
.Rhistory
.Rproj.user/

# LaTeX build files
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls

# PDF outputs (regenerable from .tex)
docs/*.pdf

# OS files
.DS_Store
Thumbs.db

# Large data files (not versioned — download separately)
data/shom_celerite_2018.csv

# Generated figures (reproductibles via src/main.R) — racine du dépôt uniquement
/figures/

# Large data outputs (keep raw data in data/)
*.rds
!data/*.rds

# Archive (old files moved here during cleanup)
_archive/
resources/
